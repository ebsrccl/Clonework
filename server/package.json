{
  "name": "mikrotik-agent-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {"start": "node --env-file=.env src/server.mjs", "test": "node --test test/*.test.mjs"}
}
